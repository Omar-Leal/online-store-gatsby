import Typography from 'typography';

const typography = new Typography({
	baseFontSize: "18px",
  baseLineHeight: 1.666,
  headerFontFamily: ["Playfair Display", "Helvetica Neue", "Arial"],
  bodyFontFamily: ["Playfair Display", "Roboto", "Georgia"], 
})

export default typography;